node_modules
.next
out
build
dist
coverage

# local env and demo worker state
.env*
!.env.example
.reopt-example

# Legacy private development artifacts must never become public.
.reopt-local.json
.sdk-local

# playwright
playwright-report
test-results
blob-report
.playwright

# tooling
*.tsbuildinfo
.turbo
.vercel
.DS_Store

# Synthetic source-map fixtures (scripts/sourcemap-stress.mjs). Disposable,
# and a 1100-chunk set is ~32MB.
.stress
